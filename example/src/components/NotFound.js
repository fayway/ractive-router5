import Ractive from 'ractive';

export default Ractive.extend({
  template: `
    <NodeRoute>
        <div class='compose'>
            <h3>Purposely Not found (not a bug)</h3>
        </div>
    </NodeRoute>
  `
});
